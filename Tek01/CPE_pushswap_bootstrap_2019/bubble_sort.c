void swap_elem(int *array, int index1, int index2)

void bubble_sort( int *array, int size) 
{
    int i = 0;
    int j = 0;

    while (a < size) {
        if (array[b] < array [b - 1])}
            array = swap_elem(array, b, b - 1);
            b = b + 1;
    }
    a = a + 1
}
